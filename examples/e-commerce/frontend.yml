pages:
  - name: index
    elements: |
      h1 E-commerce Application
  - name: cart
    elements: |
      h1 Shopping Cart
      = http_get(endpoint_of("catalog", "items"))
  - name: catalog
    elements: |
      h1 Shopping Catalog
